<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auction.mapper.UserAddressMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.auction.entity.UserAddress">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="receiver_name" property="receiverName" jdbcType="VARCHAR"/>
        <result column="receiver_phone" property="receiverPhone" jdbcType="VARCHAR"/>
        <result column="province" property="province" jdbcType="VARCHAR"/>
        <result column="city" property="city" jdbcType="VARCHAR"/>
        <result column="district" property="district" jdbcType="VARCHAR"/>
        <result column="detail_address" property="detailAddress" jdbcType="VARCHAR"/>
        <result column="full_address" property="fullAddress" jdbcType="VARCHAR"/>
        <result column="is_default" property="isDefault" jdbcType="INTEGER"/>
        <result column="tag" property="tag" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, user_id, receiver_name, receiver_phone, province, city, district,
        detail_address, full_address, is_default, tag, create_time, update_time, deleted
    </sql>

    <!-- 根据ID查询地址 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_address
        WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 查询用户的所有地址 -->
    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_address
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY is_default DESC, create_time DESC
    </select>

    <!-- 查询用户的默认地址 -->
    <select id="selectDefaultByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_address
        WHERE user_id = #{userId} AND is_default = 1 AND deleted = 0
        LIMIT 1
    </select>

    <!-- 插入地址 -->
    <insert id="insert" parameterType="com.auction.entity.UserAddress" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_address (
            user_id, receiver_name, receiver_phone, province, city, district,
            detail_address, full_address, is_default, tag
        ) VALUES (
            #{userId}, #{receiverName}, #{receiverPhone}, #{province}, #{city}, #{district},
            #{detailAddress}, #{fullAddress}, #{isDefault}, #{tag}
        )
    </insert>

    <!-- 更新地址 -->
    <update id="update" parameterType="com.auction.entity.UserAddress">
        UPDATE user_address
        <set>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverPhone != null">receiver_phone = #{receiverPhone},</if>
            <if test="province != null">province = #{province},</if>
            <if test="city != null">city = #{city},</if>
            <if test="district != null">district = #{district},</if>
            <if test="detailAddress != null">detail_address = #{detailAddress},</if>
            <if test="fullAddress != null">full_address = #{fullAddress},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
            <if test="tag != null">tag = #{tag},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 删除地址（逻辑删除） -->
    <update id="deleteById" parameterType="java.lang.Long">
        UPDATE user_address
        SET deleted = 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 取消用户的所有默认地址 -->
    <update id="cancelDefaultByUserId" parameterType="java.lang.Long">
        UPDATE user_address
        SET is_default = 0, update_time = CURRENT_TIMESTAMP
        WHERE user_id = #{userId} AND deleted = 0
    </update>

    <!-- 设置默认地址 -->
    <update id="setDefault" parameterType="java.lang.Long">
        UPDATE user_address
        SET is_default = 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 统计用户地址数量 -->
    <select id="countByUserId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM user_address
        WHERE user_id = #{userId} AND deleted = 0
    </select>

</mapper>

